name: Build & Push Tool Image to GHCR

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
    paths:
      - 'src/loaders/compute_tools/**/Dockerfile'
  push:
    branches:
      - main
      - master
      - develop
    paths:
      - 'src/loaders/compute_tools/**/Dockerfile'

jobs:
  build-push-tool-images:
    runs-on: ubuntu-latest
    steps:
      - name: Check out GitHub Repo
        uses: actions/checkout@v3

      - name: Get current date
        id: date
        run: echo "date=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: '${{ secrets.GHCR_USERNAME }}'
          password: '${{ secrets.GHCR_TOKEN }}'

      - name: Build and push tool images
        id: build-and-push
        run: |
          tools=(
            mash
#            microtrait
#            checkm2
#            gtdb_tk
          )

          for tool in "${tools[@]}"; do
            dockerfile="./src/loaders/compute_tools/$tool/Dockerfile"
            tag="ghcr.io/${{ github.event.repository.owner.login }}/${{ github.event.repository.name }}_$tool:latest"

            echo "Building and pushing $tool image..."
            echo "tagging image with $tag"

            docker buildx build --file "$dockerfile" --tag "$tag" \
              --build-arg "BUILD_DATE=${{ steps.date.outputs.date }}" \
              --build-arg "VCS_REF=${{ github.sha }}" \
              --build-arg "BRANCH=${{ github.ref }}" \
              --build-arg "TAG=${{ github.ref }}" \
              --push .
          
          done

#      - name: Build and push checkm2 tool image
#        uses: docker/build-push-action@v3
#        with:
#          context: .
#          file: ./src/loaders/compute_tools/checkm2/Dockerfile
#          pull: true
#          push: true
#          build-args: |
#            BUILD_DATE=${{ steps.date.outputs.date }}
#            VCS_REF=${{ github.sha }}
#            BRANCH='${{ github.ref }}'
#            TAG='${{ github.ref }}'
#          tags: 'ghcr.io/${{ github.event.repository.owner.login }}/${{ github.event.repository.name }}_checkm2:latest'
#
#      - name: Build and push gtdb_tk tool image
#        uses: docker/build-push-action@v3
#        with:
#          context: .
#          file: ./src/loaders/compute_tools/gtdb_tk/Dockerfile
#          pull: true
#          push: true
#          build-args: |
#            BUILD_DATE=${{ steps.date.outputs.date }}
#            VCS_REF=${{ github.sha }}
#            BRANCH='${{ github.ref }}'
#            TAG='${{ github.ref }}'
#          tags: 'ghcr.io/${{ github.event.repository.owner.login }}/${{ github.event.repository.name }}_gtdb_tk:latest'
#
#      - name: Build and push mash tool image
#        uses: docker/build-push-action@v3
#        with:
#          context: .
#          file: ./src/loaders/compute_tools/mash/Dockerfile
#          pull: true
#          push: true
#          build-args: |
#            BUILD_DATE=${{ steps.date.outputs.date }}
#            VCS_REF=${{ github.sha }}
#            BRANCH='${{ github.ref }}'
#            TAG='${{ github.ref }}'
#          tags: 'ghcr.io/${{ github.event.repository.owner.login }}/${{ github.event.repository.name }}_mash:latest'
#
#      - name: Build and push microtrait tool image
#        uses: docker/build-push-action@v3
#        with:
#          context: .
#          file: ./src/loaders/compute_tools/microtrait/Dockerfile
#          pull: true
#          push: true
#          build-args: |
#            BUILD_DATE=${{ steps.date.outputs.date }}
#            VCS_REF=${{ github.sha }}
#            BRANCH='${{ github.ref }}'
#            TAG='${{ github.ref }}'
#          tags: 'ghcr.io/${{ github.event.repository.owner.login }}/${{ github.event.repository.name }}_microtrait:latest'
