columns:
   - key: coll
     type: string
     filter_strategy: identity

   - key: load_ver
     type: string
     filter_strategy: identity

   - key: _mtchsel
     type: string
     filter_strategy: identity

   - key: kbase_id
     type: string
     filter_strategy: identity

   - key: kbase_display_name
     type: string
     filter_strategy: ngram